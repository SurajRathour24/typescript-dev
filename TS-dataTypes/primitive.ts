// Tuples 

let items: [number, string] = [1, "hey"];

console.log(items);


// enum are like objects 

enum UserRoles {
    Admin  = "Main Admin",
    Guest = "Main Guest"
}

UserRoles.Admin;

// Enum are use to reuse o like obj

// Basic DT

let age: number = 25;
let isActive: boolean = true;
let username: string = "Suraj";

console.log(age, isActive, username);